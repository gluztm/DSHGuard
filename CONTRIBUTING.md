# 参与贡献

本文件面向要改动 DSH 守护壳源码的人：只讲**怎么改、怎么验、怎么提**。

「怎么用」以及构建依赖、构建命令、源码结构表见 [README.md](README.md)，
本文件不重复那些内容，只补它没有的部分。

---

## 改之前

**一次只做一件事。** 一个改动只解决一个问题：修缺陷就只修缺陷，不额外重构、不额外改动文案。
改动范围越小，验证越快、回退越干净、评审越容易看清。本项目自身的改动按这个尺度切分。

**先确认工作区里没有别人的改动。** 动手前先看 `git status`：如果其中存在未做过的修改，
应先弄清楚它们的来路，不得将自己的改动与它们混在一起。并发改同一个文件时，后写的一方会静默
覆盖先写的一方，而这种覆盖在提交历史里看不出来。

---

## 怎么改

- **改动范围尽量单一**：能改一个方法就不改一个类，能改一个类就不动公共接口。
- **不夹带无关改动**：顺带格式化、顺带修改注释、顺带调整顺序，都算另一件事，另开一次改动。
- **行为变了就同步自检**：自检断言的是现有行为。行为按设计改了，断言要一起更新；
  行为没变而断言变红，说明改动有问题，不是断言有问题。
- **不留半成品**：做不到的部分要么不做，要么在提交说明里写清楚哪里没做。

---

## 怎么验

### 自检：任何改动之后都要全绿

自检是这套程序的验收基线，**不是可选项**：任何改动之后都应当自检全部通过（退出码 0）再提交。
自检不显示窗口、不访问引擎、不写入真实配置，可以随时执行；构建与自检命令见
[README.md](README.md) 的「想提交贡献？」一节。

自检会如实区分三种结果：通过、失败，以及**因环境不满足而没验**的那一类。最后一类不等于通过，
改动涉及它时请在提交说明中注明，不得视为已经验过。

### 改了界面：出图核对

自检覆盖得了结构与取值，覆盖不了「看起来对不对」。改了界面（配色、间距、圆角、文案排版、
新页签、新弹窗）之后，用截图模式出图，逐张目视核对：

```powershell
# 主界面：不显示窗口、不访问引擎；默认夜间，加 --light 出日间版
DSHGuard.exe --shot 图.png [--state 页名] [--light]

# 对话框样式：几种典型组合并排（提示 / 是-否 / 多键），加 --dark 出夜间版
DSHGuard.exe --dialog-shot 图.png [--dark]
```

`--state` 可取 `market`（社区收录，默认）、`market-filter`、`market-scrolled`、`card-zoom`、
`plugins`、`settings-version`、`settings-version-pin`、`settings-version-bottom`、
`settings-general`、`settings-paths`、`loading`、`loading-hover`、`snapshots`、`running`、
`logs`、`about`、`lightbox`；写了不认识的值会落到状态页。

这两个模式都按测试夹具运行：日志写入系统临时目录，不会写入本机日志目录。

出图之后至少核对三件事：文字没有截断或重叠、中文没有乱码、日间与夜间两套配色都成立。

### 改了打包或安装：装一次，再卸一次

改动涉及安装包、安装向导、注册表项、快捷方式或卸载程序时，必须**实际装一遍、再卸一遍**，
并确认两件事：装完之后程序能正常启动；卸完之后没有残留的注册表项与快捷方式。
卸载的两种模式（保留数据、连数据一起删）都要各走一遍。卸载说明见 [README.md](README.md)。

### 改了引擎交互或插件操作：写清现场是怎么造的

这类改动（启动与终止引擎、安装与更新插件、快照与回滚）依赖真实环境，自检覆盖不到的部分要自己
造现场验证，并在提交说明里写清造法：用的什么来源、什么版本、预期看到什么、实际看到什么。

---

## 代码约定

### 界面文案

界面文案是全项目最硬的一条约束：**一律中文**，且不出现代码片段、命令行、内部标识、盘符路径、
网址与站点专名，也不摆专业术语；措辞官方正式，面向不熟悉命令行的用户。自检里有专门扫描禁用词
的断言校验这一条，改文案时不得绕过它。

同一条约束还包含：一句只说一件事；拿不准原因时如实写「暂无法确定」，不编原因，也不写「可能是……」
这类含糊话；没有用户能执行的下一步时，那一行整行不出现，不写无实际内容的表述。

实现细节不进界面，但**不许删**：它们的去处是代码注释与日志。文案是单点实现的常量或纯函数，
改一处即全变，调用点不得各自另写一套。

### 注释

注释按标准程序化表述写，中文，说明「为什么」而不只是「做了什么」：为什么需要这个分支、为什么用
这个顺序、为什么不选更直觉的那种写法。有取舍的地方把取舍写出来。公开与内部成员用 C# 的文档注释
（`<summary>`）；自检断言引用到的实现点，要能被注释定位。

### 文件编码与行尾

仓库文本一律 **UTF-8 无 BOM + LF**，文件末尾留一个换行。这一条由两处共同保证：
`.gitattributes` 管进仓库时的行尾，`.editorconfig` 管编辑器写盘时的编码与行尾 —— 两处都要在。

**唯一的例外是 PowerShell 脚本**：它们由 Windows PowerShell 5.1 执行，而 5.1 把无 BOM 的 UTF-8
按 ANSI 解码，脚本里的中文会乱码甚至解析失败，所以带中文的脚本保留 UTF-8 BOM。哪些文件带、
哪些不带，以及为什么，写在 `.editorconfig` 的对应分节里，改脚本前先看一眼。

脚本示例一律写成 Windows PowerShell 5.1 能执行的形式，不得写只有 PowerShell 7 才支持的新语法，
也不得假定本机安装过 PowerShell 7：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File 脚本.ps1
```

---

## 怎么提

提交说明写清三件事，缺一件即无法判断：

1. **改了什么**：具体到文件与行为，不得只写「优化」「修复问题」。
2. **为什么**：原来的行为哪里不对，或者新需求是什么。
3. **怎么验证的**：自检是否执行过、结果如何；改了界面就附截图；改了打包就说明装、卸各走了一遍；
   自检覆盖不到的部分，写清是怎么手工验的。

**一次提交一件事**：把无关改动拆开。评审时看得清，出问题时回退得干净。

---

## 许可

贡献的代码按本项目的 MIT 许可证发布，全文见 [LICENSE](LICENSE)。
