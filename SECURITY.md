# 安全说明

本程序是给 DeepSeek Harness（DSH）网页引擎配的桌面守护壳。它在本机做的事情限于以下几类：

- **启动与终止引擎进程**：以本机已安装的 Node.js 拉起 DSH 引擎，并在用户要求时终止它。
- **调用包管理器改动依赖**：安装、更新、卸载 DSH 插件，走的是引擎自己的包管理命令。
- **读写自己的数据**：设置、日志与快照都存在程序目录下；引擎配置目录以只读为主，
  仅在检测到明显损坏且不动就会卡住时，先备份再修正，并在界面上说明改了什么。
- **访问网络**：检查引擎与插件的新版本、加载社区收录信息与图片、下载引擎依赖。
  程序不收集、不上报用户的使用数据。

---

## 报告安全问题

通过本仓库的 Issue 报告，或在仓库的「安全」页提交私密安全公告（Security Advisories）联系维护者。
如果问题涉及尚未修复的缺陷，请优先使用私密公告，不得在公开 Issue 中披露可复现的利用步骤。

本项目由个人维护，不设响应时限承诺；收到后会尽快处理。

---

## 用户自查

- **只从本仓库的 Releases 页面下载安装包**，不得从其它来源获取可执行文件。
- 下载后**核对 SHA256 校验值**：打包时会把安装包的校验值打印在构建输出里，与 Release 说明中
  给出的值一致，再运行安装程序。
- 若怀疑本机程序已被替换，先卸载再重新下载安装；卸载程序在开始菜单与系统「应用」列表中。
