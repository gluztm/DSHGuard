# ─────────────────────────────────────────────────────────────────────────────
# DSH 守护壳（DSHGuard）版本库属性规则
#
# 管的是「怎么存」：行尾与二进制判定。与 .gitignore（管「提交什么」）是两件事，
# 两者互不替代，也互不干扰。
#
# 为什么需要这个文件（此前一条规则都没有，完全依赖 git 自行判定）：
#   本机 core.autocrlf = true（Windows 上 git 的默认，系统级设置）。
#   没有规则时，谁在 Windows 上编辑后提交，就可能把 CRLF 掺进仓库，
#   同一个文件于是留下两种行尾的历史记录噪音；「逐字节比对」这类验证手段也就失效了
#   （本项目大量依赖逐字节比对）。
#   有了下面的规则，行尾不再取决于每台机器的 autocrlf，而是仓库自己说了算。
#
# 本文件只管两件事：① 行尾一律 LF；② 二进制一律明说。
# 不管 BOM：BOM 是「文件头三个字节」，行尾是「行末一个字节」，两件独立的事。
#   .ps1 的 BOM 现状不统一，且两种均为有意设置：
#     带 BOM —— tools\install-node.ps1、tools\port-check.ps1、installer\build-installer.ps1
#               （Windows PowerShell 5.1 读取带中文的 UTF-8 脚本需要它，去掉会导致中文字符显示异常）；
#     不带 BOM —— tools\check-plugin-updates.ps1、tools\clean-logs.ps1
#               （已按设计写成纯 ASCII，不依赖 BOM，去掉也不会损坏）。
#   本文件既不添加也不移除任何 BOM。
# ─────────────────────────────────────────────────────────────────────────────

# ── 兜底：逐字节判定为文本的文件，一律按 LF 存取 ──
# text=auto 让 git 按内容判定（含 NUL 即二进制），避免逐个扩展名声明而遗漏；
# eol=lf 必须显式写死：只写 text=auto 的话，行尾会随各人 autocrlf 发生变化，
#   而「任何人 clone 到的仓库内容都确定一致」正是本文件要达到的效果。
# 注意：已有文本文件在索引里本来就是 LF，这条规则对它们不产生作用，
#   不会触发重新规范化（否则 git status 会整片报告 M）。
* text=auto eol=lf

# ── 文本：显式点名，逐条写死 LF ──
# 上面兜底已按内容判定，这里再按扩展名显式声明一次，是为了让意图可读、
# 且不依赖 git 的 NUL 探测结果（探测会随 git 版本和启发式变化）。

# C# 源码 / WPF 界面 / 项目文件
*.cs      text eol=lf
*.xaml    text eol=lf
*.csproj  text eol=lf
# PowerShell 脚本（tools\*.ps1、installer\build-installer.ps1；BOM 现状见文件头说明）
*.ps1     text eol=lf
# Inno Setup 安装脚本
*.iss     text eol=lf
# 文档
*.md      text eol=lf
# 无扩展名的文本：LICENSE 与 .gitignore 都属于这一类
LICENSE       text eol=lf
.gitignore    text eol=lf
# 本文件自身
.gitattributes text eol=lf
# 纯文本 / 配置（当前仓库还没有，先定好规矩，将来加了不必再补规则）
*.txt     text eol=lf
*.json    text eol=lf
*.yml     text eol=lf
*.yaml    text eol=lf
*.xml     text eol=lf
*.config  text eol=lf

# ── 二进制：显式标明，不依赖 git 判定 ──
# binary 即 -text -diff：既不规范化行尾（规范化会改字节、毁文件），
#   也不在 diff / 逐字节比对里按文本处理。
# 这些文件当前已被 git 用 NUL 检测判成 -text、从未被转换过；
# 写成显式规则是防止将来换 git 版本、换平台或启发式变化后判错。
# ICO 像素数据里天然含 0x0D 0x0A 字节对，一旦被当文本就会损坏，尤其不能依赖自动判定。
*.png     binary
*.ico     binary
# 压缩包与可执行体（当前仓库未跟踪任何此类文件，规则先立着防误伤）
*.zip     binary
*.gz      binary
*.7z      binary
*.exe     binary
*.dll     binary
*.pdb     binary
*.woff    binary
*.woff2   binary
*.ttf     binary
*.otf     binary
