# 变更日志

本文件按「变更日志」惯例组织：版本号从新到旧排列，每条按「新增 / 变更 / 修复」分类，
只列对使用者有影响的变化；没有内容的分类不出现。

版本号规则：前两位是发布号（1.0 是首个发布版），同一次发布内的小修改累加第三位。

## [1.3.4] - 2026-09-22

**新增**

- 更新进度窗新增「发布页面」按钮，点击直接打开下载页面，方便在自动下载不顺利时改为手动下载。
- 下载时间超过一分钟仍未完成时，进度百分比后面会提示网络环境不佳、建议手动下载，不必一直等到超时。

**变更**

- 更新进度窗的两颗按钮改为水平居中排列，并留出适当间距。
- 「取消下载」改为橙黄色底、白色文字，鼠标悬停时渐变为红色；「发布页面」为蓝色底、白色文字，悬停时渐变为绿色。

**修复**

- 图片缓存增加 256MB 上限：此前市场截图缓存只增不减，长期浏览会让占用持续增长。现在超过上限会自动清理最久未使用的缓存，并始终保留当前正在使用的那一张。

## [1.3.3] - 2026-09-22

**变更**

- 插件市场的分类标签默认只占一行：放不下的分类收进末尾的「更多」，点「更多」就在原地展开，把全部分类按多行铺出来，再点「收起」收回一行。
- 分类栏在向下浏览列表时自动收起，回到顶部或向上滚动时自动恢复，把纵向空间让给列表。

**修复**

- 当前选中的分类始终保留在可见的一行里：此前分类很多时，选中的那个可能被挤出可见范围，用户看不出自己正在按哪个分类筛选。

## [1.3.2] - 2026-09-22

**变更**

- 看图界面去掉图片外层的深色底与描边：悬停预览与放大图都改为让图片直接显示在暗色遮罩上，悬停预览的投影一并去掉；放大图的标题与页码各加一层很淡的半透明底，保证文字在浅色图片上仍然清晰。

**修复**

- 修复放大图只能向左上方向放大的问题：此前缩放只改变图片尺寸，没有同步调整显示位置，于是画面始终从左上角开始扩大。现在以鼠标所在位置为锚点缩放，鼠标指向的那一点在缩放前后保持不动。
- 看图界面支持按住鼠标拖动查看局部：放大后按住左键即可平移画面，查看图片的任意局部。拖动与点击按位移区分：位移很小仍视为点击（翻页或打开仓库页面），位移明显则视为拖动、不再触发点击行为。

## [1.3.1] - 2026-09-22

**修复**

- 修复应用内更新可能长时间停在某个百分比不动的问题：下载过程中若某条下载通道持续以极低速度传输数据，原有的停滞判定不会触发，界面会停在原地，直到整次更新的时间上限用完为止。现在为整个更新过程增加了总时长上限，超时后如实说明并退回下载页，不再无限期等待。
- 修复更新期间「取消下载」可能无效的问题：取消信号此前只覆盖下载环节，检查版本等步骤中点击没有反应。现在取消信号覆盖更新全过程，任意时刻都可以中止。
- 更新进度窗不再强制置顶：此前它始终显示在所有窗口之上，切换到其它程序也无法避开。现在它跟随主窗口，主窗口最小化时一并最小化，主窗口恢复时回到前面。

## [1.3] - 2026-09-21

**新增**

- 插件市场的看图界面支持缩放：按住 Ctrl 滚动滚轮即可放大或缩小，滚得越多缩放幅度越大，不按 Ctrl 时滚轮照常用于滚动其它内容。切换图片或关闭看图界面后缩放自动回到适应窗口的状态。

**变更**

- 看图界面的翻页方式改为左右两侧热区：鼠标移到图片左侧或右侧时，该侧出现一个半透明的翻页箭头，点击即可向前或向后翻。原来的左下角翻页按钮已移除，页码保留。
- 看图界面右下角的「看原图」按钮已移除，改为点击图片中间区域直接打开该插件的仓库页面；图片左右两侧各占两成宽度的区域用于翻页，中间六成用于打开仓库。图片未能取到时，提示文字也相应改成了打开仓库页面的指引。

## [1.2.3] - 2026-09-21

**新增**

- 本地插件页的「筛选」改为「排序」：可按**安装时间**、**创建日期**、**更新日期**、**兼容性**排序；同一项再次点击即反向，选中的那一行右侧显示 ↑ / ↓。默认按安装时间从新到旧。兼容性按"不兼容 → 基本可用 → 完全兼容 → 未声明"固定顺序，不随方向翻转（它表达的是风险高低，不是大小）。
- 新增两项本地记账：**安装时间**（本机某插件是什么时候装上的）与**更新日期**（本机最近一次更新它的时间），写入程序自己的配置目录，只在本机使用、不对外发送。**从本版本开始记账**，此前已安装的插件没有记录，界面显示「未知」；卸载后重装会记成最近一次安装。
- 插件卡片新增两处时间：名字后显示作者的**创建时间**（该插件首次发布的时间），右上角显示本机的**更新日期**，均按 xxxx年xx月xx日 显示。

**变更**

- 「筛选」里的兼容情况、启用状态、只看有新版三项筛选已移除；兼容情况与启用状态由排序与批量多选承担，有无新版由「一键更新 N 个」的计数体现。

**修复**

- 记一笔账只在操作确实成功时发生：安装、更新、卸载、回滚重装与批量操作失败、被中止或只完成一半时，一律不记账，避免出现"界面说成功、日期却对不上"。

## [1.2.2] - 2026-09-20

**修复**

- 修复应用内更新的下载线路表把「直连」这类**显示名**当成请求地址的问题：此前每条线路在发起请求前就被判为非法地址，四条线路瞬间全部失败、连一个数据包都没有发出。现在线路表里存的都是可直接请求的地址，并新增四条自检断言锁住这项契约。
- 修复应用内更新下载安装包时可能失败的问题：下载过程中一旦出现短暂的数据间歇，读取会被中断并破坏连接，导致下载当场失败。现在改为仅结束本次等待，连接与读取继续保留。
- 修复部分网络环境下无法下载安装包的问题：部分网络能连上代码托管站点、也能拿到响应，但数据传不下来。现在下载会依次尝试多条线路，前一条走不通时自动改用下一条；全部走不通时才退回下载页。仅在「线路本身走不通」时切换线路；若下载到的内容本身不正确，则不再重试并如实说明。
- 修复本地写入失败时反复更换线路重试的问题：安装位置无法写入、磁盘空间不足一类情况，更换下载线路不会有任何帮助，现在直接给出结论并退回下载页。
- 界面细节：插件安装与卸载进行中的「停止」按钮统一为橙色，与其它可中止操作的颜色一致。
- 代码清理：移除已不再提供的窗口分屏布局及其在说明页中的对应说明。

## [1.2.1] - 2026-09-20

**修复**

- 修复应用内更新下载安装包时连接被中途破坏、导致下载失败的问题。
- 修复部分网络环境下可以连上代码托管站点却无法取到数据、导致无法下载安装包的问题：现在会依次尝试多条下载线路。

## [1.2] - 2026-09-20

**新增**

- 插件变动期间退出时增加确认：安装、更新、卸载或回滚重装进行中，通过「退出UI」或托盘菜单退出时，会先弹窗说明中断的后果（本轮操作将被中断、可能留有未完成的中间状态、进度显示随之消失），由用户确认后再退出。批量禁用与批量启用仅写入配置、不产生中间状态，不在确认范围内。
- 更新提前结束时如实说明原因：此前因「上次安装残留未能清理」或「无法确定可靠的更新目标」而提前结束的情况下，进度显示被直接收起且无任何说明；现在会给出对应结论。

## [1.1] - 2026-09-20

**新增**

- **应用内更新**：设置 → 版本页新增「守护壳版本」卡片，可检查本程序自身有没有新版本，查到后直接在本程序内下载并安装，不必再去下载页手动取包。下载按已接收字节数显示百分比；更新期间主界面暂不可操作，异常中断会恢复；取不到包或安装位置不可写入时自动退回打开下载页。更新过程不影响正在运行的引擎。
- **断电级安全冗余**：覆盖安装期间断电或强制关机时，磁盘上始终保留完整且已校验的安装包、上一版程序的副本与双击即用的恢复脚本，用户可自行恢复；退出之前先探测安装位置是否可写，写不进去就不退出；下次启动自动核对更新结果，装成了安静收尾，没装成如实说明且只报一次。
- 从代码仓库安装或更新插件时，若本机缺少所需的版本控制工具，改为给出中文说明，不再执行注定失败的安装命令。
- 新增参与贡献、安全说明与变更日志三份文档，README 增加英文入口。

**变更**

- 卸载结论改为同时核对插件目录与插件清单，分四态如实报告（已卸载 / 未卸干净 / 未卸载 / 判不了），不再只凭包目录是否存在下结论。
- 更新或卸载插件前识别引擎状态：只有确实有会话在活动时才提醒，引擎空闲或未运行时不打扰；提醒不是禁止。
- 版本页移除「重新检测」（与「检查更新」功能完全重合），「前往下载」改为「立即更新」。

**修复**

- 卸载插件后，插件清单中的登记项未被同时移除，界面显示为「已卸载」但该条目仍在清单里，导致后续安装会把它重新装回：现在两处都清掉才算卸载完成，只清掉目录的情形如实报告为「未卸干净」。
- 引擎的写入锁在某些情况下会残留（点「停止」结束插件命令时留下），导致之后所有插件操作超时失败：现在会在改变依赖之前清理这类残留锁，且只在确认持有者已不存在时才清理。
- 编辑器写入的文件行尾不再依赖于各机器的设置：新增版本库属性规则与编辑器约定，仓库内容在任何机器上保持一致。

## [1.0] - 2026-09-20

首个公开发布版。

**这是什么**：给 DeepSeek Harness（DSH）网页引擎配的 Windows 桌面守护壳 —— 一键启动与停止引擎、
锁定引擎版本、管理插件、改动前自动存档并可整组回滚、出问题时导出诊断。全中文界面，
面向不熟悉命令行的用户。

**新增**

- 状态页：一键启动与停止引擎，显示端口、工作目录与运行状态。
- 版本记忆：锁定引擎版本、保留回退候选、升级与回退。
- 插件页：本地插件的启停、更新、卸载，以及社区收录的浏览、筛选与安装。
- 快照页：改动前自动存档，支持整组回滚与逐项还原。
- 日志页：实时输出、历史日志与「导出诊断」压缩包。
- 设置页：常规、路径、版本三项设置，含毛玻璃背景开关与数据目录调整。
- 安装包与卸载程序：用户级安装，免管理员权限。

**修复**

- 本机没有安装 Git 时，从代码仓库取回的插件无法安装或更新，而旧版本的提示未说明原因：
  现在不再执行任何命令，改为给出中文提示，说明缺少什么、下一步该做什么，并把原因写入日志。
